import React from "react";

export const Savings = (savings ) =>{
    const monthly = savings * 30
    const yearly = savings * 365

    return (
        <>
            <div>
                <p></p>
            </div>
        </>
    )
}